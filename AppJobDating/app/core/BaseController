<?php
namespace App\app\core;

class BaseController
{        
    // name view and data
    protected function view($view, $data = [])
    {
        extract($data);
        $checkView =  BASE_PATH  . '/app/views/' . $view . '.php';
         // view not exist
        if (!file_exists($viewFile)) {
            die("View not found: {$viewPath}");
        }
        require $viewFile;
    }
}